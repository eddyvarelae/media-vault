# Runbook: archive `kipp` (B26) - draft 2026-09-17T21:48:31-07:00, PM

Status: **B47 + B48 merged `d276012` = `v0.2.7` (2026-09-19 01:44). Dry-run 2 (Tester rev 6) once the `v0.2.7` image is published and the script (now `kipp-<folder>` disk names) is re-placed on the NAS. Dry-run 1 (2026-09-19 01:00, Tester #32) would have copied 4,804 of 9,872 - see B47/B48.** Step 1 done: `kipp` = `/usb/sdc1`.

## Facts (Tester #19, Reviewer #8; #18 for the NAS)
- `kipp`: 10,393 files / 1.95 TB; **9,872 files / 1,921,695,784,449 B need archiving**; 521 already archived by content (520 = all of `LeanTank/`, 1 in `Backup/`).
- Folders needing archive: `SonyA6700` 5,978 (582.5 GB), `Backup` 3,648 (260.0 GB), `Multicam` 34 (426.2 GB), `Auditorium` 10 (336.2 GB), `GoPro/DCIM` 184 (230.9 GB), `SonyZVE10` 18 (85.9 GB).
- **13 files** share `(source_path, size)` with archived rows but differ in content (11 `SonyA6700/DCIM/*.ARW` with wrapped counters, `Backup/PRIVATE/DATABASE/DATABASE.BIN`, two AVCHD/XML index files). Under a **shared** `media-*` disk name a copy **skips** these (`Verified, changed`, `INCOMPLETE:`, exit 1) - the old file always wins. **B48 removes the need for a separate pass**: kipp copies under `kipp-*` disk names, so these are new rows that `--on-collision rename-mtime-year` lands beside the originals in the main pass (step 6 kept only if Eddy wants the 13 handled separately).
- NAS: `sudo docker run` as `figmaboi`; USB disks appear under `/mnt/@usb/<dev>` (`tars` was `/usb/sdc1` inside the container); image `ghcr.io/eddyvarelae/media-vault:v0.2.7` - every NAS script defaults to it since #74 (`2af22d3`) (`VAULT_IMAGE` overrides only for a one-off).
- Nothing writes `/volume1/media` except a `vault copy` **Eddy** launched (TEAM.md sacred paths). Any `vault` command against the live manifest = a deploy: one at a time, lock note, heartbeat.

## Steps
1. **Eddy** plugs `kipp` into a NAS USB port. **Tester** (SSH, read-only): `ls -la /mnt/@usb/`, `mount | grep usb`, top-level listing of the kipp mount, confirm which `sdXN` it is and that the 7 folders are there. Report as an item.
2. **Flags - done (Tester #23, 2026-09-17T21:51:13-07:00).** Historical layouts: Sony trees verbatim (no flags); `Backup`, `LeanTank` verbatim; GoPro = `--prefix DCIM --rule MP4=Videos --rule LRV=Videos --rule THM=Videos --rule JPG=Photos --rule sav=Other`. `kipp`'s layout maps 1:1: `SonyA6700`, `SonyZVE10`, `Backup` → no flags; `GoPro/DCIM` flat → the GoPro line; `LeanTank` → all archived (the run will skip every file; keep the line so the record shows it); `Multicam`, `Auditorium` → new disks, verbatim (no precedent). Every historical run used `--on-collision rename-mtime-year`; none had `--dedupe-content` (did not exist) - the kipp run is the first with it and the first with a pinned image tag.
3. **Dev**: `scripts/nas-ssd-copy-all.sh kipp` (B36: generalized from the tars/kipp scripts, `<label>` = `tars`|`kipp`): `IMG` default `v0.2.6`, `SRC="${SSD_SRC:?}"` for kipp (the container path from step 1), one `run_copy` per folder with `--dedupe-content --on-collision rename-mtime-year`, per-camera flags from step 2, `DRY_RUN=1` passes `--dry-run` to every call, single-logging helper (B27), `LOG` override, `DOCKER` knob (B46); docker-stub test. **B48: kipp copies under `kipp-<folder>` disk names** (`kipp-sonya6700`, `kipp-backup`, `kipp-multicam`, `kipp-auditorium`, `kipp-gopro`, `kipp-sonyzve10`, `kipp-leantank`), each into `/volume1/media/<Folder>` — new rows, so its Sony 4-digit names land beside tars/noahsarc/case originals via `rename-mtime-year` instead of being refused by B23(b), and LeanTank records its dedupe rows. Reviewed (#18; B46/B48 after).
3a. **Place the script on the NAS** (note: UGOS chroots SFTP at `/volume1`; use `scp -O`) (after B46 merges): Tester as `vaultagent`: `scp scripts/nas-ssd-copy-all.sh vaultagent@192.168.1.167:/volume1/docker/nas-ssd-copy-all.sh` from the merged `main` SHA, `chmod +x`, `sha256sum` on both sides in the note. `/volume1/docker/` is mode 777 - no sudo needed; nothing else there is touched. Explicit PM line required (it is a write).
4. **Dry-run** (as `vaultagent`, no `sudo -E`: `cd /volume1/docker && SSD_SRC=/usb/sdc1 DOCKER="sudo -n docker" DRY_RUN=1 ./nas-ssd-copy-all.sh kipp 2>&1 | tee -a /volume1/docker/kipp-dryrun.log` - B46 knob) - runs the script against the live manifest with `--dry-run`: no archive file, no manifest row; the log is appended and the manifest is opened (B31). Who runs it: DECISIONS NEEDED (PM recommends the Tester as `figmaboi`, under a lock note, since it writes nothing). **Acceptance:** per folder, `Copying N files (X B)` must equal the gap-report counts/bytes for that folder (Reviewer #8 table), `Verified, changed:` must list exactly the 13, `Dst owned` / `Dst via symlink` 0. Tester posts the arithmetic; Reviewer recomputes.
5. **Real copy** - **Eddy** launches (`nohup … &`), lock note by the PM, Tester glances the log every ~30 min (heartbeat), ~3 h at USB speed. Ends `all kipp copies done`; per-folder exit 1 on `SonyA6700` and `Backup` is **expected** (the 13 skipped) - the Tester confirms the `INCOMPLETE:` lines name only those.
6. **The 13**: Eddy copies them (preserving relative paths) into `/volume1/docker/kipp-collisions/<Folder>/…` (150 MB), then one `copy kipp-sonya6700 /volume1/docker/kipp-collisions/SonyA6700 /volume1/media/SonyA6700 --on-collision rename-mtime-year` (and `kipp-backup` for the DATABASE/index files) - they land beside the originals as `*_2026.*`. Or decide to drop them (Eddy). 
7. **Verify + certify** = B6 (after B24 repair-dest lands): `verify … --only-unverified` per disk, then `certify --root … ` to `/volume1/docker/vault-certs/`. **B48: this must cover the `kipp-*` disks too** (kipp copies under `kipp-<folder>` names into the same `/volume1/media/<Folder>` trees); `scripts/nas-verify-certify-all.sh` lists them commented-out, to enable once the kipp copy has written rows (a disk with no rows makes `certify` exit 1).
