# PM resume note - written 2026-09-17T23:19:08-07:00 (session still running; use if the PM session is lost)

Boot lines: `team/archive/2026-09-17-pm-handoff.md` (unchanged; Dev window 406, Tester 407 as of this session; ids change on reboot). Framework: v2.3 applied 2026-09-17 - run startup step 0 first.

## Pending, in order
| Item | State | Next | Owner |
|---|---|---|---|
| Reviews #24 restore, #25 certs-out resolution, #26 tagger, #27 small-fixes, #28 backup | staged, Codex limit until **02:35 Sep 18** (cron reminder 02:38 in the PM session - re-create if the session was lost) | run one at a time `</dev/null`; merge on APPROVE in order #25, #24, #26, #27, #28 (Dev merges `main` where conflicts) | PM |
| B24 live repair (195 rows) | Reviewer-accepted (#23), `v0.2.2` published, runbook `team/context/runbook-b24.md` | Eddy names the executor (DECISIONS NEEDED 5) → run → verify → Tester witnesses | Eddy → PM/Tester |
| B26 `kipp` copy | script merged (#19), runbook `team/context/runbook-kipp.md` steps 1-7 | Eddy plugs `kipp` into the NAS → Tester identifies the mount → dry-run (executor per decision 5) → Reviewer checks counts vs gap numbers → Eddy launches the copy | Eddy → Tester → PM |
| B36 `tars` copy | numbers reviewed (#14); same shape as kipp, script not written | after kipp; Dev writes `nas-tars-copy-2.sh` (or parametrize the kipp script) | PM → Dev |
| B40 restore of `DSC04868_2025.JPG` | tool built (#24 pending); source on EMV `Backups/SonyA6700/DCIM/DSC04868.JPG` sha `b7ecf808…` | after #24 merges + tag `v0.2.3`: PM-logged act on the NAS with the file staged to `/volume1/docker/` (EMV is on the Mini) | PM |
| B18 tagger LaunchAgent | after #26 merges | Tester's `observed` 3-file run from the PM checkout → PM installs the plist (one act) | Tester → PM |
| B22 backup LaunchAgent | after #28 merges | same pattern | Tester → PM |
| B6 verify + certify all | needs B24 done; `certs-out` merged (certs to `/volume1/docker/vault-certs/`, `--root`) | runbook to write; Eddy runs, Tester witnesses; then remove the six in-tree certs (PM act) | PM |
| B39 605 empty-dest rows | Eddy's memory question (Apr 27/28 evening) open | repair with `repair-dest`-shaped tool later | PM |

## Open questions to Eddy (DECISIONS NEEDED, all asked)
1-4: `kipp` plugged in? `tars` reused as a dump disk Apr-Jul? who runs the kipp dry-run? what ran on the NAS Apr 27/28 evening? 5: who executes the B24 live repair (PM recommends the Tester as a one-off write under lock). 6: Codex credits vs wait (moot at 02:35).

## Traps (don't re-derive)
- Bash tool shell is zsh: unquoted `$var` does not word-split; use arrays. `find` is `bfs`.
- Codex: `</dev/null` mandatory; ~11 reviews per usage window; never two at once (same file). Don't grep its stdout for "usage limit" - the file content echoes.
- Merges: channel files conflict every time (keep both sides, `python3` union); code conflicts → back to Dev, never resolved by the PM.
- Timestamps in messages to Eddy: read `date`, never estimate.
- The Tester works in the PM checkout for channel writes and pulls `main`; a momentary conflict there resolves itself.
