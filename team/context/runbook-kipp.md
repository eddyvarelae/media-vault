# Runbook: archive `kipp` (B26) - draft 2026-09-17T21:48:31-07:00, PM

Status: **draft, not approved to run.** Steps 3-5 pass the Reviewer before Eddy types anything.

## Facts (Tester #19, Reviewer #8; #18 for the NAS)
- `kipp`: 10,393 files / 1.95 TB; **9,872 files / 1,921,695,784,449 B need archiving**; 521 already archived by content (520 = all of `LeanTank/`, 1 in `Backup/`).
- Folders needing archive: `SonyA6700` 5,978 (582.5 GB), `Backup` 3,648 (260.0 GB), `Multicam` 34 (426.2 GB), `Auditorium` 10 (336.2 GB), `GoPro/DCIM` 184 (230.9 GB), `SonyZVE10` 18 (85.9 GB).
- **13 files** share `(source_path, size)` with archived rows but differ in content (11 `SonyA6700/DCIM/*.ARW` with wrapped counters, `Backup/PRIVATE/DATABASE/DATABASE.BIN`, two AVCHD/XML index files). Under `v0.2.1` a `media-sonya6700` copy **skips** these (`Verified, changed`, `INCOMPLETE:`, exit 1) - the old file always wins. They need a second pass under their own disk name (step 6).
- NAS: `sudo docker run` as `figmaboi`; USB disks appear under `/mnt/@usb/<dev>` (`tars` was `/usb/sdc1` inside the container); image `ghcr.io/eddyvarelae/media-vault:v0.2.1` (scripts still default to `v0.2.0` → pass `VAULT_IMAGE`, B37).
- Nothing writes `/volume1/media` except a `vault copy` **Eddy** launched (TEAM.md sacred paths). Any `vault` command against the live manifest = a deploy: one at a time, lock note, heartbeat.

## Steps
1. **Eddy** plugs `kipp` into a NAS USB port. **Tester** (SSH, read-only): `ls -la /mnt/@usb/`, `mount | grep usb`, top-level listing of the kipp mount, confirm which `sdXN` it is and that the 7 folders are there. Report as an item.
2. **Tester** (read-only, mini-server logs): the exact `vault copy` flags (`--prefix`, `--rule`) the Sep 1 `case-copy` run used for `Backup`, `LeanTank`, `SonyZVE10`, `SonyA6700`, and the Apr 27 `tars` run for `GoPro` (in `scripts/nas-tars-copy-all.sh`: `--prefix DCIM --rule MP4=Videos --rule LRV=Videos --rule THM=Videos --rule JPG=Photos --rule sav=Other`). The kipp copy must produce the same layout per camera.
3. **Dev**: `scripts/nas-kipp-copy-all.sh` - same shape as `nas-tars-copy-all.sh`: `IMG` default `v0.2.1`, `SRC="${KIPP_SRC:?}"` (the container path from step 1), one `run_copy` per folder with `--dedupe-content --on-collision rename-mtime-year`, per-camera flags from step 2, **new disks** `media-multicam` → `/volume1/media/Multicam` and `media-auditorium` → `/volume1/media/Auditorium`, `DRY_RUN=1` passes `--dry-run` to every call, single-logging helper (B27), `LOG` override; docker-stub test. Reviewed (#18).
4. **Dry-run** (`DRY_RUN=1`) - runs the script against the live manifest with `--dry-run` (no archive file, no manifest row; B31 caveat: it opens the manifest). Who runs it: DECISIONS NEEDED (PM recommends the Tester as `figmaboi`, under a lock note, since it writes nothing). **Acceptance:** per folder, `Copying N files (X B)` must equal the gap-report counts/bytes for that folder (Reviewer #8 table), `Verified, changed:` must list exactly the 13, `Dst owned` / `Dst via symlink` 0. Tester posts the arithmetic; Reviewer recomputes.
5. **Real copy** - **Eddy** launches (`nohup … &`), lock note by the PM, Tester glances the log every ~30 min (heartbeat), ~3 h at USB speed. Ends `all kipp copies done`; per-folder exit 1 on `SonyA6700` and `Backup` is **expected** (the 13 skipped) - the Tester confirms the `INCOMPLETE:` lines name only those.
6. **The 13**: Eddy copies them (preserving relative paths) into `/volume1/docker/kipp-collisions/<Folder>/…` (150 MB), then one `copy kipp-sonya6700 /volume1/docker/kipp-collisions/SonyA6700 /volume1/media/SonyA6700 --on-collision rename-mtime-year` (and `kipp-backup` for the DATABASE/index files) - they land beside the originals as `*_2026.*`. Or decide to drop them (Eddy). 
7. **Verify + certify** = B6 (after B24 repair-dest lands): `verify media-* --only-unverified` per disk, then `certify --root … ` to `/volume1/docker/vault-certs/`.
